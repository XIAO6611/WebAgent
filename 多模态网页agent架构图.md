下面给你提供的是**可以直接放进开题报告/PPT 的系统架构图方案**。
我会给你 **3 个层级的架构图**，分别对应老师常见评分点：

1）**总体系统架构图（最重要，必放 PPT）**
2）**Agent 内部模块架构图（体现你们理解系统设计）**
3）**数据流 / 执行流程图（体现多模态与自动操作）**

这些图都是按你们项目 **携程 + 小红书 WebAgent** 量身设计的，符合课程要求里的：

- VLM（界面理解）
- LLM（规划动作）
- 工具调用（Playwright）
- 状态管理

你可以直接复制到 **draw.io / ProcessOn / PPT / Visio** 里画。

------

# 一、总体系统架构图（最重要，必须有）

这是**开题报告核心图**。
如果老师只看一张图，基本就是这一张。

------

# 系统总体架构图（推荐直接使用）

```text
                ┌──────────────────────────┐
                │          User            │
                │  输入自然语言任务          │
                │  "帮我找成都500元以内酒店" │
                └────────────┬─────────────┘
                             │
                             ▼
                ┌──────────────────────────┐
                │      Task Planner        │
                │        (LLM)             │
                │  任务理解 + 操作规划       │
                └────────────┬─────────────┘
                             │
                             ▼
                ┌──────────────────────────┐
                │     Perception Module    │
                │          (VLM)           │
                │      网页截图理解         │
                └────────────┬─────────────┘
                             │
                             ▼
                ┌──────────────────────────┐
                │     Action Generator     │
                │       动作生成模块         │
                │   click / scroll / type  │
                └────────────┬─────────────┘
                             │
                             ▼
                ┌──────────────────────────┐
                │    Browser Executor      │
                │       (Playwright)       │
                │     执行浏览器操作         │
                └────────────┬─────────────┘
                             │
                             ▼
        ┌────────────────────────────────────────┐
        │                Websites                │
        │                                        │
        │   携程（酒店搜索）      小红书（用户评价） │
        │                                        │
        └────────────────────────────────────────┘
```

------

# 这一张图老师会看什么？

主要看：

```text
是否有：

VLM
LLM
Browser automation
真实网站
```

你这张图：

```text
完全符合题目要求
```

------

# 二、Agent 内部模块架构图（体现系统设计能力）

这一张图通常放在：

```text
系统设计章节
```

老师会觉得：

```text
你们理解 Agent 结构
```

------

# Agent 模块架构图

```text
                ┌──────────────────────────┐
                │        Web Agent         │
                └──────────────────────────┘

     ┌────────────────────────────────────────────┐
     │                                            │
     │              Core Modules                  │
     │                                            │
     └────────────────────────────────────────────┘

     ┌──────────────────┐
     │   Perception      │
     │  感知模块          │
     │------------------│
     │ 获取网页截图        │
     │ 识别按钮/文本        │
     │ 提取页面信息        │
     └──────────────────┘

     ┌──────────────────┐
     │     Planner       │
     │   任务规划模块      │
     │------------------│
     │ 理解用户任务        │
     │ 分解操作步骤        │
     │ 生成动作序列        │
     └──────────────────┘

     ┌──────────────────┐
     │    Executor       │
     │   执行模块          │
     │------------------│
     │ click()           │
     │ scroll()          │
     │ type()            │
     └──────────────────┘

     ┌──────────────────┐
     │     Memory        │
     │   状态管理模块      │
     │------------------│
     │ 当前页面            │
     │ 历史操作            │
     │ 任务状态            │
     └──────────────────┘
```

------

# 这一张图体现什么？

老师会看到：

```text
你们不是简单脚本
而是 Agent 系统
```

这是一个关键加分点。

------

# 三、多模态数据流图（非常关键）

这一张图专门用来回答：

```text
你们的多模态在哪里？
```

很多组会被问这个问题。

这张图就是答案。

------

# 多模态数据流 / 执行流程图

```text
User 输入任务
        │
        ▼
LLM 解析任务
        │
        ▼
打开浏览器
        │
        ▼
获取网页截图
        │
        ▼
        ┌──────────────────────┐
        │   Vision Model (VLM) │
        │                      │
        │  识别页面元素          │
        │  例如：                │
        │  - 搜索框              │
        │  - 价格                │
        │  - 评分                │
        └──────────────────────┘
                     │
                     ▼
           生成下一步操作
                     │
                     ▼
        ┌──────────────────────┐
        │   Playwright 执行操作 │
        │                      │
        │  click()             │
        │  type()              │
        │  scroll()            │
        └──────────────────────┘
                     │
                     ▼
                新网页状态
                     │
                     ▼
               重复直到完成任务
```

------

# 这一张图的作用

老师问：

```text
你们的多模态在哪里？
```

你直接指：

```text
网页截图 → VLM
```

就结束了。

------

# 四、你们项目的真实技术架构（可写报告）

这是一个更贴近工程实现的版本。

------

```text
                User
                  │
                  ▼
        ┌──────────────────┐
        │   Streamlit UI   │
        │  输入用户任务       │
        └────────┬─────────┘
                 │
                 ▼
        ┌──────────────────┐
        │      LLM         │
        │  GPT-4o-mini     │
        │  Qwen2.5         │
        └────────┬─────────┘
                 │
                 ▼
        ┌──────────────────┐
        │   Agent Controller│
        │                  │
        │  任务管理           │
        │  状态管理           │
        └────────┬─────────┘
                 │
                 ▼
        ┌──────────────────┐
        │    Playwright    │
        │ 浏览器自动化工具    │
        └────────┬─────────┘
                 │
                 ▼
        ┌──────────────────┐
        │   Chrome Browser │
        │                  │
        │  携程网站          │
        │  小红书网站         │
        └────────┬─────────┘
                 │
                 ▼
        ┌──────────────────┐
        │   Screenshot     │
        │   网页截图         │
        └────────┬─────────┘
                 │
                 ▼
        ┌──────────────────┐
        │   Vision Model   │
        │    (VLM)         │
        │  识别页面内容       │
        └──────────────────┘
```

------

# 五、你们 PPT 建议放这 2 张图就够了

不要太多。

------

# 必放

### 1）系统总体架构图

（最重要）

### 2）多模态数据流图

（证明你们是多模态）

------

# 如果时间充足再放

### Agent 模块架构图

------

# 六、我建议你下一步做这件事（优先级最高）

直接实现：

```python
page.screenshot()
```

这是：

```text
多模态系统最关键第一步
```

